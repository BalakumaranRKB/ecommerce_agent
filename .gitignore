# Secrets — never commit real keys
.env

# Virtual environment
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# SQLite runtime database
cost_ledger.db

# AgentCore local cache / build output
.bedrock_agentcore/
.bedrock_agentcore.yaml

# Eval output and captured evidence — regenerate rather than commit.
eval/results/

# docs/ is personal scaffolding (PLAN, phase implementation plans, progress tracker)
# Kept local; only core CWPs and Defensibles reach the reviewer.
docs/*
!docs/autonomy-decision.md
!docs/dropped-handoff-found.md
!docs/hitl-bug-found.md
!docs/pii-leak-found.md
!docs/images/