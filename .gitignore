# Secrets — never commit real keys
.env

# Virtual environment
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# docs/ is personal scaffolding (PLAN, AWS primer) — not a deliverable, kept local.
# The one exception is the confident-wrong-path writeup: §2.2 requires that case
# reach the reviewer, so it's un-ignored explicitly.
docs/
!docs/confident-wrong-path.md

# Eval output and captured evidence — regenerate rather than commit.
eval/results/
